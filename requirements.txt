# Baby Project Manager - Runtime dependencies
# Compatible with Windows, Linux, and macOS.
# Install with: pip install -r requirements.txt
#
# These are also declared in pyproject.toml ([project.dependencies]); keep the
# two in sync. Build tools live in requirements-build.txt, dev/test tools in
# requirements-dev.txt.

# Core GUI Framework
PySide6>=6.4.0

# Data processing (Excel import path)
pandas>=1.5.0

# Excel file support
openpyxl>=3.0.10

# PDF processing and extraction
pdfplumber>=0.7.0
PyPDF2>=3.0.0

# Java integration for Microsoft Project files
# Note: Requires a Java Runtime Environment
jpype1>=1.4.0
mpxj>=10.0.0

# Calendar and working days calculation
workalendar>=16.0.0

# HTTP requests for the auto-updater
requests>=2.28.0

# Windows-specific (optional): uncomment for startup-management features
# pywin32>=306; sys_platform == "win32"

# macOS-specific (optional):
# pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"

# Note: pathlib, unicodedata, configparser, platform, datetime, subprocess,
# shutil, os, sys, re, ast and math are part of the Python standard library.
